# Same Client ID as SHOPIFY_API_KEY in /app/server/.env — safe to expose to
# the browser, it's the public app identifier App Bridge needs.
SHOPIFY_API_KEY=your_api_key_here

# Port the Express backend is running on locally.
BACKEND_PORT=8080
